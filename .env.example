# WIX to ElevenLabs Ingest System - Environment Configuration

# Server Configuration
PORT=3000

# Redis Configuration (Production - optional, will use in-memory if not set)
# Get these from Upstash Redis dashboard: https://console.upstash.com
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# Lead Data TTL (Time To Live in seconds)
# Default: 86400 (24 hours)
LEAD_TTL_SEC=86400

# WIX Webhook Security (Optional)
# Set this to a random string to validate webhook authenticity
# WIX should include this in X-Webhook-Secret header or ?secret= parameter
WIX_WEBHOOK_SECRET=your_long_random_secret_string_here

# ElevenLabs API Configuration (REQUIRED for outbound calls)
# Get your API key from: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Get your Agent ID from ElevenLabs dashboard - it's the ID of your "Penny" agent
# Found in: https://elevenlabs.io/app/conversational-ai -> Select Agent -> Agent ID in URL or settings
ELEVENLABS_AGENT_ID=your_agent_id_here

# Development Notes:
# - If Redis credentials are not provided, the system will use in-memory storage
# - In-memory storage is suitable for development but not for production
# - WIX_WEBHOOK_SECRET is optional but recommended for production security
# - ELEVENLABS_API_KEY and ELEVENLABS_AGENT_ID are REQUIRED for automatic outbound calling
# - Without ElevenLabs config, the system will only store leads but won't initiate calls